{
  "name": "email-agent-system",
  "version": "1.0.0",
  "description": "Full-stack serverless email agent system with dashboard and API for business outreach",
  "main": "api/index.ts",
  "scripts": {
    "dev": "vercel dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:api": "vercel dev --listen 3001",
    "build": "cd frontend && npm run build",
    "deploy": "vercel",
    "install:all": "npm install && cd frontend && npm install",
    "test": "echo 'Tests to be implemented'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "@vercel/node": "^3.2.17",
    "googleapis": "^140.0.1",
    "openai": "^4.67.1",
    "validator": "^13.12.0",
    "dotenv": "^16.4.5",
    "axios": "^1.7.7",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "@types/validator": "^13.12.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}